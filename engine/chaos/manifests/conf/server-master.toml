etcd-endpoints = ["metastore-business:12479"] # Reuse the same endpoints as the business-metastore.

[framework-metastore-conf]
endpoints = ["metastore-framework:13306"]
auth.user = "root"
auth.passwd = ""
schema = "test"

[business-metastore-conf]
endpoints = ["metastore-framework:13306"]
auth.user = "root"
auth.passwd = ""
schema = "test"
