apiVersion: v1
kind: Service
metadata:
  name: metastore-mysql
  labels:
    app: metastore-mysql
spec:
  ports:
    - name: port-mysql # note the name is no more than 15 characters
      port: 13306
      targetPort: 3306
  selector:
    app: metastore-mysql

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: metastore-mysql
  labels:
    app: metastore-mysql
spec:
  selector:
    matchLabels:
      app: metastore-mysql
  serviceName: metastore-mysql
  replicas: 1
  podManagementPolicy: Parallel
  template:
    metadata:
      labels:
        app: metastore-mysql
    spec:
      containers:
        - name: metastore-mysql
          image: mysql:5.7
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - mountPath: "/var/lib/mysql"
              name: metastore-mysql
          env:
            - name: MYSQL_ALLOW_EMPTY_PASSWORD
              value: "true"
          ports:
            - containerPort: 3306
              name: port-mysql
          args:
            - "--server-id=1"
  volumeClaimTemplates:
    - metadata:
        name: metastore-mysql
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi


---
apiVersion: v1
kind: Service
metadata:
  name: metastore-etcd
  labels:
    app: metastore-etcd
spec:
  ports:
    - name: port-etcd # note the name is no more than 15 characters
      port: 12479
      targetPort: 2479
  selector:
    app: metastore-etcd


---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: metastore-etcd
  labels:
    app: metastore-etcd
spec:
  selector:
    matchLabels:
      app: metastore-etcd
  serviceName: metastore-etcd
  replicas: 1
  podManagementPolicy: Parallel
  template:
    metadata:
      labels:
        app: metastore-etcd
    spec:
      containers:
        - name: metastore-etcd
          image: quay.io/coreos/etcd:v3.5.4
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - mountPath: "/data/etcd"
              name: metastore-etcd
          ports:
            - containerPort: 2479
              name: port-etcd
          command:
            - "etcd"
          args:
            - "--name=metastore-etcd"
            - "--data-dir=/data/etcd"
            - "--advertise-client-urls=http://metastore-business:12479"
            - "--listen-client-urls=http://0.0.0.0:2479"
            - "--listen-peer-urls=http://127.0.0.1:2480"
            - "--initial-advertise-peer-urls=http://127.0.0.1:2480"
            - "--initial-cluster=metastore-etcd=http://127.0.0.1:2480"
            - "--initial-cluster-state=new"
  volumeClaimTemplates:
    - metadata:
        name: metastore-etcd
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
