# diff Configuration.

check-thread-count = 4

export-fix-sql = true

check-struct-only = false

dm-addr = "http://127.0.0.1:8261"

dm-task = "test"

[task]
    output-dir = "./output"
    target-check-tables = ["*.*"]
