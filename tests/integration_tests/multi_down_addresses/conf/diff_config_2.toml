# diff Configuration.

check-thread-count = 4

export-fix-sql = true

check-struct-only = false

[task]
    output-dir = "/tmp/tidb_cdc_test/multi_down_addresses/sync_diff/output"

    source-instances = ["up"]

    target-instance = "down"

    target-check-tables = ["multi_down_addresses.?*"]

[data-sources]
[data-sources.up]
    host = "127.0.0.1"
    port = 4000
    user = "root"
    password = ""

[data-sources.down]
    host = "127.0.0.1"
    port = 3307
    user = "root"
    password = ""
